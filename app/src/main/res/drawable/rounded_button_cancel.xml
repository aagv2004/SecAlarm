<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/secondaryColor" /> <!-- Color del botón -->
    <corners android:radius="16dp" /> <!-- Bordes redondeados -->
    <stroke
        android:width="2dp"
        android:color="@color/primaryColor" /> <!-- Borde opcional -->
</shape>
